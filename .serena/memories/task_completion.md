# Task Completion Checklist
- [ ] Run tests if applicable: `pytest`
- [ ] Verify application startup: `python app.py` checks for DB init and config loading.
- [ ] If changing DB schema, ensure migration or DB re-init compatibility (SQLite/PostgreSQL compatibility).