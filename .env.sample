# Sample environment variables for FE-master
# Copy this file to `.env` and fill in real values before starting the app.

# Flask / app
FLASK_ENV=production
DEBUG=false
SECRET_KEY=change_this_to_a_secure_value
PORT=5000
HOST=0.0.0.0

# Admin
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_admin_password

# Database (MySQL example for AWS RDS)
# Format: mysql://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL=mysql://admin:YOUR_DB_PASSWORD@YOUR_DB_HOST:3306/fe_exam

# Optional explicit DB pieces (used by Config parsing if needed)
DB_HOST=YOUR_DB_HOST
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=YOUR_DB_PASSWORD

# Other
# If you use file uploads on EC2, leave uploads folder. If you switch to S3, set S3_BUCKET etc.
#UPLOADS_DIR=uploads
