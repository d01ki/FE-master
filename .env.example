# Database Configuration
# For Render PostgreSQL:
DATABASE_URL=postgresql://username:password@hostname:5432/database_name
# or for local development:
# DATABASE_URL=sqlite:///fe_exam.db

# Flask Configuration - REQUIRED in production
SECRET_KEY=your-secret-key-here-change-this-in-production-use-random-string
FLASK_ENV=production

# Admin Account (Required for first deployment)
# After creating admin account, you can remove these from environment
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-admin-password-here

# Application Settings
DEBUG=False
PORT=5002
HOST=0.0.0.0

# Render.com deployment notes:
# 1. Set these environment variables in your Render dashboard
# 2. Make sure DATABASE_URL points to your Render PostgreSQL instance
# 3. Generate a strong SECRET_KEY (use: python -c "import secrets; print(secrets.token_hex(32))")
# 4. Set a secure ADMIN_PASSWORD for the admin account
